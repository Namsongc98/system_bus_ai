# 0.7 — Quy ước response list: `PageResponse<T>{content, page, size, totalElements, totalPages}` trong `common-library`

Plan: `.claude/docs/plan/screen-feature-plan.md` · Index: `.claude/docs/report/screen-feature-plan.md`
Date: 2026-09-24 · Lead review: round 1, CLEAN · Review doc: `.claude/docs/review/0.7-page-response.md`

## Kết quả
- DoD (plan): record `PageResponse<T>` (`page` 0-based) + `from(Page)` / `from(Page, mapper)`, JSON đúng 5 key; `PageRequests.of(page, size)` chặn `page < 0`, `size < 1`, `size > 100` → 400; không chuyển endpoint cũ → đạt, kiểm bằng `PageResponseTest` (6 test, gồm test JSON đúng 5 key) và `PageRequestsTest` (5 test).
- S1: done — record `PageResponse<T>` với 5 field, `content` không bao giờ `null` (compact constructor).
- S2: done — `from(Page)`, `from(Page, mapper)`; số liệu lấy từ accessor của `Page` nên `Page` unpaged không lỗi.
- S3: done — `PageRequests.of(int page, int size)`, `MAX_SIZE = 100`, vượt → `IllegalArgumentException` (→ 400 qua `GlobalExceptionHandler`), không kẹp (D1).
- S4: done — `PageResponseTest`.
- S5: done — `PageRequestsTest`.

## Endpoint thay đổi
| Method | Path | Auth | Change |
|---|---|---|---|
| — | — | — | Không có. `GET /api/bus` (1.1), `GET /api/trip/scheduled` (1.3), `GET /api/salary` (4.4) chuyển sang `PageResponse` ở task tương ứng |

## File thay đổi
**ticket-system** (branch at report time: `task/0.6-auth-me`)
- `common-library/src/main/java/com/ticket_system/common/Dto/response/PageResponse.java` — mới: record + factory
- `common-library/src/main/java/com/ticket_system/common/util/PageRequests.java` — mới: helper `Pageable` có giới hạn `size`
- `common-library/src/test/java/com/ticket_system/common/Dto/response/PageResponseTest.java` — mới
- `common-library/src/test/java/com/ticket_system/common/util/PageRequestsTest.java` — mới

**root repo** (branch at report time: `task/0.6-auth-me`)
- `.claude/docs/review/0.7-page-response.md` — mới: spec review + lead review round 1
- `.claude/docs/report/0.7-page-response.md` — mới: report này
- `.claude/docs/report/screen-feature-plan.md` — thêm dòng 0.7 vào mục lục
- `.claude/docs/plan/screen-feature-plan.md` — DoD dòng 0.7
- `.claude/ledger/screen-feature-plan.md` — tick `spec/implement/test/review` của 0.7 [mixed — also contains tick `lead-review` của 0.6 do user]

## Test
- `mvn -q -f ticket-system/pom.xml -pl manage-revenue-ticket -am test` → exit 0; `common-library` tests=12 failures=0 errors=0; `manage-revenue-ticket` tests=69 failures=0 errors=0.
- `mvn -q -f ticket-system/pom.xml -pl common-library install -DskipTests` → exit 0.
- FE không đổi → không chạy test FE.

## Review
- `backend-code-review` (self-review, trong `plan-task`): 1 finding low (L1, `content` null qua constructor public) → đã sửa + test. Không cần review độc lập vì không chạm auth/security.
- Lead review round 1: CLEAN.

## Còn lại
- DEFER: L2 → task 1.1 (test qua controller: `size=101` → 400); L3 → B18 (body lỗi 400 dạng `Map`).
- NEEDS-USER: none

## Commit
- **booking_ticket_vue:** no files — không test, không commit.
- **ticket-system:** branch `task/0.7-page-response` (tạo từ HEAD `task/0.6-auth-me` → nhánh 0.7 chồng lên 0.6), commit `9956f4d`, 4 file, không có mixed file. Test trước commit: `mvn -q -f ticket-system/pom.xml -pl manage-revenue-ticket,booking_ticket -am test` → exit 0; `common-library` 12/0 fail, `manage-revenue-ticket` 69/0 fail, `booking_ticket` 0 test.
- **root repo:** branch `task/0.7-page-response` (tạo từ HEAD `task/0.6-auth-me`), 5 file; mixed: `.claude/ledger/screen-feature-plan.md`.
