# 0.6 — `GET /api/auth/me`

Plan: `.claude/docs/plan/screen-feature-plan.md` · Index: `.claude/docs/report/screen-feature-plan.md`
Date: 2026-09-23 · Lead review: round 5, CLEAN · Review doc: `.claude/docs/review/0.6-auth-me.md`

## Kết quả
- DoD (plan): trả `id, email, role, fullName, phone` của người gọi (`id` từ JWT, `email`/`role` từ `users`, `fullName`/`phone` từ `profiles`, null nếu chưa có); `authStore.fetchMe` được gọi thật sau login/register và khi chưa có user; lỗi JWT (401/404) → xoá toàn bộ localStorage/sessionStorage/cookie và về login → đạt. Chứng minh bằng `AuthControllerTest` (200 có/không profile, 401 thiếu/sai JWT, 404 user không còn, không lộ `password`), `AuthServiceTest`, `authStore.spec.js`, `axiosAuth.spec.js`, `resetStore.spec.js`.
- Quyết định user (2026-09-23): D1 thêm `phone`; D2 chỉ lỗi JWT mới xoá phiên, mọi lỗi JWT về login và xoá toàn bộ cookie/session/localStorage.
- S1 `CurrentUserResponse` record — done
- S2 `ProfileRepository.findByUserId` — done
- S3 `AuthService.getCurrentUser` (`@Transactional(readOnly = true)`, 404 khi user không còn) — done
- S4 `GET /api/auth/me` trong `AuthController` — done
- S5 `fetchMe` được gọi thật (`resolveCurrentUser`, `ensureSession`) — done
- S6 `normalizeUser` không lấy `sub` làm email, map `sub` → `id` — done
- S7 xử lý lỗi `fetchMe` theo D2 + `clearSession()` xoá toàn bộ dữ liệu client — done
- S8 test controller — done
- S9 test service — done
- S10 test store/axios — done
- S11 axios 401 (trừ login/register) → xoá phiên + về LOGIN — done
- Thêm từ lead review: fallback JWT nằm trong `fetchMe` (L1); reset mọi Pinia store khác khi xoá phiên (L12, L19); huỷ request đang bay khi xoá phiên (L18); thông báo "phiên đã kết thúc" thay cho "canceled" (L25).

## Endpoint thay đổi
| Method | Path | Auth | Change |
|---|---|---|---|
| GET | `/api/auth/me` | JWT bất kỳ role (`AuthInterceptor`; không `@PublicApi`/`@RoleRequired`) | Mới. Response `BaseResponseDto<CurrentUserResponse{id, email, role, fullName, phone}>`; 401 thiếu/sai JWT; 404 user của JWT không còn |

## File thay đổi
**ticket-system** (branch at report time: develop)
- `manage-revenue-ticket/src/main/java/com/ticket_system/manage_revenue_ticket/Dto/response/CurrentUserResponse.java` — DTO mới (S1)
- `manage-revenue-ticket/src/main/java/com/ticket_system/manage_revenue_ticket/repository/ProfileRepository.java` — `findByUserId` (S2)
- `manage-revenue-ticket/src/main/java/com/ticket_system/manage_revenue_ticket/service/AuthService.java` — inject `ProfileRepository`, `getCurrentUser` (S3)
- `manage-revenue-ticket/src/main/java/com/ticket_system/manage_revenue_ticket/controller/AuthController.java` — `GET /me` (S4)
- `manage-revenue-ticket/src/test/java/com/ticket_system/manage_revenue_ticket/controller/AuthControllerTest.java` — 5 test `/me` (S8)
- `manage-revenue-ticket/src/test/java/com/ticket_system/manage_revenue_ticket/service/AuthServiceTest.java` — 3 test `getCurrentUser` (S9)

**booking_ticket_vue** (branch at report time: develop)
- `src/stores/auth.js` — `fetchMe`/`ensureSession`/`resolveCurrentUser` theo D2, `normalizeUser`, `clearSession()` huỷ request + xoá storage + reset store (S5–S7, L1, L12, L18, L19)
- `src/services/axios.js` — 401 → `clearSession` + LOGIN (trừ login/register), gắn signal phiên, thông báo huỷ (S11, L2, L18, L25)
- `src/utils/storage.js` — `clearClientData()` (S7)
- `src/main.js` — đăng ký `resetStorePlugin` (L12)
- `src/services/sessionAbort.js` — mới, `AbortController` theo phiên (L18)
- `src/stores/plugins/resetStore.js` — mới, `$reset` cho setup store, registry theo Pinia (L12, L19)
- `src/__tests__/authStore.spec.js` — mới (S10, L3, L4, L12, L13, L18)
- `src/__tests__/axiosAuth.spec.js` — mới (S10, L2, L3, L18, L25)
- `src/__tests__/resetStore.spec.js` — mới (L19)

**root repo** (branch at report time: develop)
- `.claude/docs/review/0.6-auth-me.md` — spec review + lead review 5 vòng
- `.claude/docs/report/0.6-auth-me.md` — file này
- `.claude/docs/report/screen-feature-plan.md` — dòng mục lục 0.6
- `.claude/docs/plan/screen-feature-plan.md` — dòng 0.6 (BE + FE, DoD mới) + blocker B21–B24
- `.claude/ledger/screen-feature-plan.md` — 0.6 `spec` (user), `implement`, `test`, `review`

## Test
- `mvn -f ticket-system/pom.xml -pl manage-revenue-ticket -am test` → exit 0, 69 tests, 0 errors, 0 failures (gồm context thật `ManageRevenueTicketApplicationTests`, `FlywaySchemaValidationTest` với MySQL local)
- `npm run test:unit -- --run` (trong `booking_ticket_vue`) → 23 files, 137 tests passed
- `npm run build` → built OK
- Chạy `npx vitest run` ở thư mục gốc `System_bus/` cho 23 file fail — do `npx` tải vitest 5.0.1 không đọc `vitest.config.js`; không phải lỗi code

## Review
- 5 vòng lead review (user cho thêm vòng 4 và 5 sau giới hạn 3 vòng). Mỗi vòng sửa đều được review độc lập bởi `frontend-reviewer` + `security-reviewer`; vòng 1 thêm `backend-reviewer` + `test-gap-reviewer`.
- Sửa chính: fallback JWT trong `fetchMe` (L1), chỉ miễn 401 cho login/register (L2), test lỗi mạng/register (L3, L4), reset store khác khi xoá phiên (L12), test lỗi JWT ngay sau login/register (L13), huỷ request đang bay (L18), registry theo Pinia (L19), thông báo huỷ rõ ràng (L25).

## Còn lại
- DEFER: L5, L6 → B21; L7 → B19; L8–L11, L14, L20–L22, L24, L26, L27 → B22; L15 → 2.x; L16 → B23; L17 → 0.1; L23 → B24; L28 → ghi nhận (chưa có task i18n)
- NEEDS-USER: none

## Commit
- **booking_ticket_vue**: branch `task/0.6-auth-me`, commit `cbb3acd`, 9 files, no mixed files. Test trước commit: `npm --prefix booking_ticket_vue run test:unit -- --run` → 23 files, 137 tests passed.
- **ticket-system**: branch `task/0.6-auth-me`, commit `ef88f07`, 6 files, no mixed files. Test trước commit: `mvn -f ticket-system/pom.xml -pl manage-revenue-ticket,booking_ticket -am test` → exit 0; `manage-revenue-ticket` 69 tests, `common-library` 1 test, 0 errors/failures; `booking_ticket` không có test class.
- **root repo**: branch `task/0.6-auth-me` (docs, không chạy test).
- Nhánh task tạo từ HEAD hiện tại (`develop` của mỗi repo).
