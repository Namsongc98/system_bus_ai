# 0.8 — Sửa check sức chứa (B4): đếm theo `trip_id`, `>=`, loại `CANCELLED` + migration V2 unique `(trip_id, active_seat_number)` + stub báo ADMIN khi ghế đã huỷ được đặt lại (N1)

Plan: `.claude/docs/plan/screen-feature-plan.md` · Index: `.claude/docs/report/screen-feature-plan.md`
Date: 2026-09-24 · Lead review: round 1, CLEAN · Review doc: `.claude/docs/review/0.8-capacity-unique-seat.md`

## Kết quả
- DoD (plan): đếm vé chưa huỷ theo chuyến + `>=`, check ghế ở create/update/loyalty, hết chỗ/trùng ghế → 409, ghế ngoài `1..capacity` → 400, chuyến 2 của cùng xe vẫn đặt được, insert trùng ghế → `DataIntegrityViolationException` (`uk_tickets_trip_active_seat`), đặt lại ghế đã huỷ → `SeatRebookedEvent` + `StaffNotificationService` chỉ log → đạt. Kiểm bằng `TicketSeatConstraintTest` (6, MySQL 8.0 Testcontainers), `TicketServiceTest` (14), `FlywaySchemaValidationTest` (3), `GlobalExceptionHandlerConflictTest` (1).
- S1: done — `countActiveTicketsByTripId`; xoá `countTicketsByBusId`, `countTicketFollowBus`.
- S2: done — `existsActiveSeat(tripId, seat, excludeTicketId)` (native count + `default` method).
- S3: done — `assertCanBookSeat` / `assertSeatFree` ở `createTicket`, `createTicketByLoyalty`, `updateTicket` (khi đổi ghế); cả 3 method `@Transactional`. Kiểm khoảng ghế áp dụng cả khi đổi ghế lúc sửa vé.
- S4: done — `V2__tickets_unique_active_seat.sql` (cột generated `STORED` + unique), không map vào entity.
- S5: done — `saveSeat`: `saveAndFlush`, `DataIntegrityViolationException` của `uk_tickets_trip_active_seat` → `ConflictException`; lỗi khác ném lại.
- S6: done — `FlywaySchemaValidationTest` kiểm 2 migration + constraint mới.
- S7: done — `TicketSeatConstraintTest` (a)–(e) + `existsCancelledSeat`.
- S8: done — `TicketServiceTest`.
- S9: done — `GlobalExceptionHandlerConflictTest` (gọi trực tiếp handler).
- S10: done — `SeatRebookedEvent`, `StaffNotificationService` (`@TransactionalEventListener(AFTER_COMMIT)`, log id, `TODO(4.6 Firebase)`), `existsCancelledSeat`.
- S11: done — 3 case event trong `TicketServiceTest`.
- Ngoài scope (bắt buộc theo plan-task): `TicketService` chuyển sang constructor injection, bỏ 4 dependency không dùng.

## Endpoint thay đổi
| Method | Path | Auth | Change |
|---|---|---|---|
| POST | `/api/ticket` | ADMIN | Hết chỗ 404 → 409; trùng ghế → 409 (trước lưu được / 500); ghế ngoài `1..capacity` hoặc `null` → 400 |
| PUT | `/api/ticket/{id}` | ADMIN | Đổi sang ghế đã có người → 409; ghế ngoài khoảng → 400 |
| POST | `/api/ticket/loyalty_reward/{idReward}` | mọi JWT (B20, chưa đổi) | Cùng check như `POST /api/ticket` |

Body lỗi giữ dạng `{status, error, message, path, timestamp}`.

## File thay đổi
**ticket-system** (branch at report time: `task/0.7-page-response`)
- `manage-revenue-ticket/src/main/resources/db/migration/V2__tickets_unique_active_seat.sql` — mới: cột `active_seat_number` + `uk_tickets_trip_active_seat`
- `manage-revenue-ticket/src/main/java/com/ticket_system/manage_revenue_ticket/repository/TicketRepository.java` — query đếm theo chuyến, ghế đang dùng, ghế đã huỷ; xoá 2 query cũ
- `manage-revenue-ticket/src/main/java/com/ticket_system/manage_revenue_ticket/service/TicketService.java` — check ghế/sức chứa, `@Transactional`, bắt lỗi unique, phát event, constructor injection
- `manage-revenue-ticket/src/main/java/com/ticket_system/manage_revenue_ticket/service/SeatRebookedEvent.java` — mới: record event
- `manage-revenue-ticket/src/main/java/com/ticket_system/manage_revenue_ticket/service/StaffNotificationService.java` — mới: stub thông báo (log + TODO 4.6)
- `common-library/src/main/java/com/ticket_system/common/exception/ConflictException.java` — mới
- `common-library/src/main/java/com/ticket_system/common/exception/GlobalExceptionHandler.java` — handler 409 [mixed — also contains đổi CRLF → LF ở 5 dòng import/annotation, không đổi nội dung]
- `manage-revenue-ticket/src/test/java/com/ticket_system/manage_revenue_ticket/migration/FlywaySchemaValidationTest.java` — expect V1 + V2, constraint mới
- `manage-revenue-ticket/src/test/java/com/ticket_system/manage_revenue_ticket/repository/TicketSeatConstraintTest.java` — mới
- `manage-revenue-ticket/src/test/java/com/ticket_system/manage_revenue_ticket/service/TicketServiceTest.java` — mới
- `common-library/src/test/java/com/ticket_system/common/exception/GlobalExceptionHandlerConflictTest.java` — mới

**root repo** (branch at report time: `task/update-skill-git-commit`)
- `.claude/docs/review/0.8-capacity-unique-seat.md` — mới: spec review + lead review round 1
- `.claude/docs/report/0.8-capacity-unique-seat.md` — mới: report này
- `.claude/docs/report/screen-feature-plan.md` — thêm dòng 0.8 vào mục lục
- `.claude/docs/proplem-business/0.8-seat-cancel-rebook.md` — mới: mô phỏng nghiệp vụ D1–D4, N1
- `.claude/docs/plan/screen-feature-plan.md` — B4, DoD dòng 0.8, thêm task 4.6 + câu hỏi mở Firebase
- `.claude/skills/plan-task/SKILL.md` — thêm 4.6 vào Task Registry
- `.claude/ledger/screen-feature-plan.md` — tick 0.8 spec/implement/test/review, thêm section 4.6 [mixed — also contains user tick `lead-review` của 0.7]

## Test
- `mvn -q -f ticket-system/pom.xml -pl manage-revenue-ticket -am test` (lead-review, 2026-09-24) → exit 0; `common-library` 13/13 pass; `manage-revenue-ticket` 89/89 pass.
- `mvn -q -f ticket-system/pom.xml install -DskipTests` → exit 0.
- FE không đổi → không chạy test FE. Chưa khởi động service trên DB local.

## Review
- `plan-task`: `backend-code-review` (self-review), không có finding high/medium. Không cần review độc lập vì không chạm auth/security.
- Lead review round 1: CLEAN, 0 FIX.

## Còn lại
- DEFER: L3 → B18 (test 409 qua MockMvc); L4 → 4.2 (đổi ghế cho vé đã huỷ); L5 → 4.3 / B20 (`createTicketByLoyalty` không set `statusTicket`); L6 → 4.6 (Firebase thật).
- NEEDS-USER: L1 — chọn thời điểm chạy V2 trên prod (dựng lại bảng `tickets`); L2 — chạy query kiểm tra trùng ghế (D4) trên DB local/prod trước khi deploy.

## Commit
- **booking_ticket_vue:** no files — không commit, không chạy test FE.
- **ticket-system:** branch `task/0.8-capacity-unique-seat` (tạo từ `task/0.7-page-response` → chồng lên commit 0.7 `9956f4d`), commit `90d63af`, 11 file. Mixed: `GlobalExceptionHandler.java` (CRLF → LF ở 5 dòng). Test trước commit: `mvn -f ticket-system/pom.xml -pl manage-revenue-ticket,booking_ticket -am test` → exit 0; `common-library` 13/13, `manage-revenue-ticket` 89/89, `booking_ticket` 0 test.
- **root repo:** branch `task/0.8-capacity-unique-seat`, hash trong chat. Mixed: ledger (kèm tick `lead-review` 0.7 của user).
