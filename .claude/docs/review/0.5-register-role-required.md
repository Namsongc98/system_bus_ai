# Spec review 0.5 — Chặn tự đăng ký ADMIN (B11) + gắn `@RoleRequired` (B12) (`manage-revenue-ticket`)

- Task: 0.5 (Phase 0, BE, size S), không phụ thuộc task nào. Là tiền đề cho
  1.1, 1.2 (qua 1.1), 1.3 (qua 1.1/1.2), 3.2.
- Blocker trong plan: **B11** (tự đăng ký làm ADMIN được), **B12** (không phân
  quyền theo role ở controller quản trị).
- DoD trong plan: Register với `role=ADMIN` → tài khoản vẫn là CUSTOMER;
  CUSTOMER gọi `POST /api/bus` → 403.
- Ngày review: 2026-09-22. Task foundation nên không review page, chỉ đọc lại
  `AuthService`, `AuthController`, `UserRequestDto`, và **toàn bộ** controller
  trong `manage-revenue-ticket/.../controller/` (không chỉ 4 controller B12
  liệt kê — xem mục 5).
- Task 0.4 (khoá `/api/ticket/summary/excel`) đã `implement`+`test`+`review`
  trong ledger, nên `TicketController` đã có 1 method dùng `@RoleRequired`
  làm mẫu tham chiếu, cùng với `AdminDashboardController`.
- Hai điểm cần quyết định đã hỏi và được xác nhận: **(1)** gộp
  `RouteController` vào fix scope dù B12 không nêu tên; **(2)** khoá luôn 3
  endpoint "bán vé trực tiếp" của `TicketController` về ADMIN ngay bây giờ.

---

## 1. Summary

| Hạng mục | Hiện tại | Sau 0.5 |
|---|---|---|
| B11 — `POST /api/auth/register` với `role=ADMIN` trong body | Tài khoản được tạo với role ADMIN thật (`AuthService.java:33` lấy role từ body) | Luôn tạo CUSTOMER, bỏ qua `role` trong body |
| B12 — `BusController` (GET/POST/PUT) | Không có `@RoleRequired` — CUSTOMER gọi được hết | `@RoleRequired(ADMIN)` toàn bộ |
| B12 — `RouteController` (POST/PUT) | Không có `@RoleRequired` (không nằm trong bằng chứng gốc của B12 nhưng cùng lỗi) | `@RoleRequired(ADMIN)` toàn bộ |
| B12 — `TripController` (POST/GET `/scheduled`/PUT/GET `/{id}/revenue`) | Không có `@RoleRequired` | `@RoleRequired(ADMIN)` toàn bộ |
| B12 — `RevenueController` (9 GET report) | Không có `@RoleRequired` | `@RoleRequired(ADMIN)` toàn bộ |
| B12 — `TicketController.addTicket/updateTicket/getTicketSummaryByBusAndTime` | Không có `@RoleRequired` | `@RoleRequired(ADMIN)` (method-level, giữ nguyên 2 method `@PublicApi` và method excel đã khoá ở 0.4) |
| `TicketController.createTicketByLoyalty` | Không có `@RoleRequired` | Không đổi — chặn bởi open decision loyalty (Phase 4.3) |
| `UserController` | Class rỗng | Không đổi — thuộc 1.2 |

Đếm: B11 còn nguyên (1) · B12 còn nguyên ở **5 controller / 17 method**
(BusController 3, RouteController 2, TripController 4, RevenueController 9,
TicketController 3 trong số 3 method còn lại chưa khoá — excel đã khoá ở 0.4).
Bằng chứng gốc của B12 chỉ nêu tên 4/5 controller (thiếu `RouteController`).

---

## 2. Bằng chứng (thay cho FE-BE matrix, vì task không có FE)

| Nội dung | Bằng chứng | Ghi chú |
|---|---|---|
| B11 còn nguyên | `manage-revenue-ticket/.../service/AuthService.java:33`: `UserRole role = (user.getRole() == null) ? UserRole.CUSTOMER : user.getRole();` | Lấy thẳng role client gửi khi có |
| DTO không chặn `role` | `manage-revenue-ticket/.../Dto/request/UserRequestDto.java:17`: `private UserRole role; // optional` — không có validation nào giới hạn giá trị | |
| Endpoint public | `AuthController.java:38-40`: `POST /api/auth/register` có `@PublicApi` | |
| `AuthService.register()` chỉ có 1 caller | Grep toàn repo: chỉ `AuthController.register` gọi `authService.register(req)` | An toàn khi sửa — không phá caller khác |
| Pattern tạo user cho ADMIN sau này | Plan dòng 1.2: "Tách `POST /api/user` (ADMIN tạo được DRIVER/COLLECTOR; register public chỉ cho CUSTOMER)" | Xác nhận hướng sửa: register public luôn CUSTOMER, endpoint tạo role khác là việc của 1.2, không phải 0.5 |
| `@RoleRequired` hiện có ở đâu | `AdminDashboardController.java:19,28,38` (class + method), `TicketController.java:47` (`exportTicketSummaryToExcel`, thêm ở 0.4) | 2 nơi duy nhất trong toàn bộ `manage-revenue-ticket` |
| `BusController` không có role check | `BusController.java:24` (`GET`), `:38` (`POST`), `:44` (`PUT /{busId}`) — không `@RoleRequired`, không `@PublicApi` | Đòi JWT hợp lệ (bất kỳ role) qua `AuthInterceptor`, nhưng không đòi ADMIN |
| `RouteController` không có role check | `RouteController.java:18` (`POST`), `:25` (`PUT /{routeId}`) | Cùng dạng lỗi như `BusController`, không nằm trong danh sách bằng chứng gốc của B12 |
| `TripController` không có role check | `TripController.java:24` (`POST`), `:30` (`GET /scheduled`), `:41` (`PUT /{tripId}`), `:53` (`GET /{tripId}/revenue`) | |
| `RevenueController` không có role check | `RevenueController.java:22,33,44,58,68,76,82,88,94` — 9 method `GET`, toàn bộ là report | |
| `TicketController` phần còn lại | `:40` `addTicket` (POST), `:73` `updateTicket` (PUT), `:80` `getTicketSummaryByBusAndTime` (GET `/summary`) — không `@RoleRequired`. `:89` `createTicketByLoyalty` (POST) cũng không, nhưng để riêng (mục 4) | |
| `UserController` | `UserController.java` — class rỗng, không có method nào để gắn annotation | Khớp B6, thuộc 1.2 |
| DoD kiểm chứng bằng đọc code | `BusController.postBus` không annotation → CUSTOMER có token hợp lệ gọi `POST /api/bus` hôm nay trả 200, không phải 403 | Đúng như DoD mô tả |

**2 việc phụ phát hiện khi đọc (không phải B11/B12, không phải fix item riêng,
chỉ dọn kèm khi đã đụng vào đúng file/dòng đó):**

- `AuthService.java:16`: field `private UserService userService;` không
  `@Autowired`, không được đọc ở bất kỳ đâu trong class — dead code.
- `TripController.java:21-22` có field `@Autowired private TripService
  tripService;`, **và** constructor `:49-51` gán lại đúng field đó
  (`public TripController(TripService revenueTripService) { this.tripService
  = revenueTripService; }`) — thừa nhưng không gây lỗi (constructor injection
  thắng field injection).
- `AuthController.register`/`login` (`:41-44`, `:68-71`) tạo `User user = new
  User()` rồi gọi `user.setRole(...)` nhưng biến `user` không được dùng ở đâu
  khác — object thật lấy từ `authService.register(req)`/`login(req)`.

---

## 3. Fix scope cho `plan-task 0.5`

### BE — `manage-revenue-ticket`

| ID | Việc |
|---|---|
| S1 | `AuthService.java`: bỏ nhánh đọc `user.getRole()`, luôn gán `UserRole.CUSTOMER` trong `register()`. Nhân tiện chuyển field injection (`userRepository`) sang constructor injection (`final`), xoá field `userService` chết (đang đụng file này) |
| S2 | `BusController.java`: thêm `@RoleRequired(UserRole.ADMIN)` ở class, lặp lại ở từng method (theo mẫu `AdminDashboardController`) |
| S3 | `RouteController.java`: thêm `@RoleRequired(UserRole.ADMIN)` ở class + từng method |
| S4 | `TripController.java`: thêm `@RoleRequired(UserRole.ADMIN)` ở class + từng method. Nhân tiện xoá field `@Autowired` thừa (giữ constructor injection đã có sẵn) vì đang đụng file |
| S5 | `RevenueController.java`: thêm `@RoleRequired(UserRole.ADMIN)` ở class + 9 method |
| S6 | `TicketController.java`: thêm `@RoleRequired(UserRole.ADMIN)` **ở method** (không phải class) cho `addTicket`, `updateTicket`, `getTicketSummaryByBusAndTime`. Không đụng `createTicketByLoyalty` |

### Tests

| ID | Việc |
|---|---|
| S7 | `AuthServiceTest` (mới): gọi `register()` với `UserRequestDto.role = UserRole.ADMIN` → user được lưu (mock `UserRepository.save`, bắt argument) có `role == CUSTOMER` |
| S8 | Test coverage cho `@RoleRequired` trên 4 controller còn lại (S2-S5): 1 test class mới (vd. `RoleRequiredCoverageTest`) hoặc 4 file riêng theo đúng mẫu `AdminDashboardControllerTest`/`TicketControllerTest` hiện có — reflection assert từng method có `@RoleRequired(value = {ADMIN})`. Chọn cách nào do lúc implement quyết, miễn giữ được mẫu reflection-check đã dùng trong repo |
| S9 | Verify thủ công (cần server chạy, không chạy được trong sandbox review này): `POST /api/auth/register` với body `{"email":...,"password":...,"role":"ADMIN"}` → tài khoản tạo ra login được và JWT có `role=CUSTOMER`. `POST /api/bus` với token CUSTOMER → 403. Cùng request với token ADMIN → hành vi cũ (200/201) |

### FE
Không có.

---

## 4. Out of scope

| Phát hiện | Thuộc |
|---|---|
| `TicketController.createTicketByLoyalty` (`POST /loyalty_reward/{idReward}`) vẫn không có `@RoleRequired` — ai được gọi (CUSTOMER tự đổi điểm hay ADMIN) chưa chốt | Phase 4.3 (mở: "Loyalty: bao nhiêu vé = 1 điểm, dùng điểm đổi gì?") |
| `UserController` vẫn là class rỗng, không có method để gắn `@RoleRequired` | 1.2 |
| `BaseLoyaltyPointsController`, `LoyaltyPointsController`, `LoyaltyRewardController`, `BaseSalaryController`, `SalaryController` — cũng không có `@RoleRequired` nào, cùng dạng lỗi B12 nhưng không thuộc 4 controller B12 nêu tên và không có screen đang active (Loyalty = 4.3, Salary = 4.4 "deferred: no screen exists") | 4.3 / 4.4 (4.4 không có task tracking theo ledger note) |
| `TripController` field injection thừa + constructor trùng (mục 2) | Dọn kèm S4, không phải fix riêng |
| `AuthController` biến `user` chết ở `register`/`login` | Không có task, cleanup nhỏ, không chặn DoD |
| `AuthController.updatePassword` (`PUT /api/auth/update-password`) không có `@RoleRequired` lẫn `@PublicApi` | ~~Đúng ý định (tự đổi mật khẩu của chính mình, không cần ADMIN) — không phải lỗi B12~~ **Đính chính 2026-09-23 (lead review L4):** kết luận cũ sai. Việc thiếu `@RoleRequired` thì đúng ý định (mọi role đã đăng nhập đều được tự đổi mật khẩu), nhưng code **không** giới hạn "của chính mình": `AuthService.updatePassWord` tìm tài khoản theo `email` trong body, không theo `id` trong JWT → user A biết mật khẩu hiện tại của B thì đổi được mật khẩu B. Lúc spec-review chỉ suy ra ý định từ annotation, không đọc tới service. Theo quyết định của user, đã sửa trong 0.5 (fix round 1): lấy tài khoản từ JWT, bỏ email khỏi DTO, thêm `@Valid` |

---

## 5. Đề xuất sửa plan

1. **B12 liệt kê thiếu 1 controller.** Bằng chứng gốc của B12 ghi "`@RoleRequired`
   = 0 lần trong `BusController`, `TripController`, `TicketController`,
   `RevenueController`" — bỏ sót `RouteController`, cùng màn hình 1.1 với
   `BusController`, cùng mức độ nghiêm trọng (POST/PUT tạo/sửa tuyến không
   role check). Đề xuất thêm `RouteController` vào câu B12 để lần đọc sau
   không bỏ sót lại.
2. **Thêm dòng cho các controller Loyalty/Salary vào B12 hoặc mục Out of
   scope của Phase 4**, để khi 4.3/4.4 triển khai không quên phân quyền —
   hiện Phase 4 open-decision chỉ nói về rule nghiệp vụ, chưa nói về
   `@RoleRequired`.

---

## 6. Open decisions

Không còn mục nào cần `(TODO/needs confirmation)` — 2 điểm cần quyết định
(gộp `RouteController`, khoá 3 endpoint bán vé trực tiếp của
`TicketController`) đã được xác nhận trước khi viết doc này (mục đầu trang).
`createTicketByLoyalty` cố tình để lại mở, không phải vì thiếu bằng chứng mà
vì rule nghiệp vụ thuộc Phase 4.3, đã ghi ở mục 4.

---

## 7. Lead review

Round 2 · 2026-09-23 · Verdict: **CLEAN** — no `FIX` open

Round 2 basis: working tree of `ticket-system` branch `task/0.5-register-role-required`
(from `develop`, uncommitted) — no longer the `10b0bde` copy used in round 1. Reviews on
record for round 2: independent `security-reviewer` on fix round 1 (L4, found L12) and on
the L12 fix (all 11 handlers of the 5 controllers ADMIN-gated; only update-password,
`LoyaltyPointsController` and `createTicketByLoyalty` stay open to any logged-in role).

Ledger: `spec` ✓ `implement` ✓ `test` ✓ `review` ✓ `lead-review` ☐ (user ticks).

**Where the code is.** The checked-out `ticket-system` branch `develop` (`99bab9c`)
does **not** contain 0.5 except S6 (bundled into the 0.4 commit). S1–S5, S7, S8 live
only in commit `10b0bde` ("task/0.4-lock-public-endpoints") on branch
`task/0.4-lock-public-endpoints`, already pushed to origin, mixed with unrelated work
(admin dashboard, Flyway V1, Kong/TLS/k8s/CI infra, `AuthInterceptor` rewrite). This
round reviewed and tested a `git archive 10b0bde` copy in the session scratchpad, not
the working tree.

**Reviews on record.** No review output for 0.5 existed before this round (the ledger
ticks `review` but nothing was recorded). This round ran independent
`security-reviewer`, `backend-reviewer`, `test-gap-reviewer` on `10b0bde`. All three
confirm S1–S6 match §3: `register()` always saves CUSTOMER (login takes role from the
DB, no second register path, `booking_ticket` has none); `@RoleRequired(ADMIN)` on
class + every mapped method of Bus/Route/Trip/Revenue (3/2/4/9) and on the 3
`TicketController` methods; `createTicketByLoyalty` untouched; `AuthInterceptor`
registered on `/**`, resolves method-then-class, forged/unknown role → 403;
`TripController` single constructor injects correctly; no Kafka consumer or Feign
caller reaches the newly locked endpoints.

| ID | Severity | Class | Finding | Evidence (file:line / command) | Status |
|---|---|---|---|---|---|
| L1 | high | FIX | No independent review on record for an auth change (B11/B12) | ledger ticks `review`; no findings in this doc or the report | fixed (this round) — security, backend, test-gap reviewers ran on `10b0bde`; none found an in-scope defect |
| L2 | medium | FIX | S9 (manual verify) never ran, and its in-process substitute (the 0.4 precedent: `TicketExcelSecurityChainTest`) is incomplete for the DoD: (a) register with `role=ADMIN` is proven only at `AuthService` unit level — no test drives `POST /api/auth/register` through `AuthController` and checks the resulting role / JWT claim; (b) no test proves ADMIN still gets 2xx on `POST /api/bus` (only `GET` is tested with ADMIN) | `AuthServiceTest.java:22-37`; `BusControllerAuthTest.java:62-83`; no `AuthControllerTest*` in tree | fixed (fix round 1) — `AuthControllerTest.registerWithAdminRoleInBodyIssuesCustomerToken` (real `AuthService` + `AuthInterceptor`: 201, saved role CUSTOMER, access + refresh JWT role CUSTOMER) and `BusControllerAuthTest.allowsAdminTokenOnBusCreation` (ADMIN → 200, `save` called). Live S9 curl still not run: the backend the user started (`:8082`) runs the old `develop` code (no-token `/api/bus` body = old interceptor's "Request processing failed: UnauthorizedRoleException"), and registering with `role=ADMIN` against it would create a real ADMIN account — rerun S9 after restarting on `task/0.5-register-role-required` |
| L3 | medium | NEEDS-USER | 0.5 has no reviewable/revertible commit of its own: S6 is inside the 0.4 commit, S1–S5/S7/S8 inside a catch-all commit on the (pushed) 0.4 branch together with unrelated work; `develop` lacks them. `plan-commit` for 0.5 cannot build a `task/0.5-…` branch from the current working tree, and a fix round must start from a tree that contains `10b0bde` | `git -C ticket-system log --all --oneline`; `git diff develop task/0.4-lock-public-endpoints --stat` (48 files); 0.4 commit message "Mixed files" | resolved (user decision) — `ticket-system` branch `task/0.5-register-role-required` from `develop` (`99bab9c`); 0.5 files taken from `10b0bde` (`RevenueController` re-applied by hand on the `develop` version: 5 methods, the 4 revenue-report endpoints of `10b0bde` belong to other work and were not carried). Uncommitted; `plan-commit` commits it. Prerequisites carried, not 0.5: see L11 |
| L4 | medium | NEEDS-USER | (security) `PUT /api/auth/update-password` picks the target account by `email` in the body, not the JWT `id`; any logged-in user can change another account's password if they know its current one. §4 filed it as "self-service, not a bug" — the code does not enforce "self". Also no `@Valid` on the body | `AuthController.java:52-61`; `AuthService.java:48-61` | fixed (user decision, fix round 1) — account from JWT `@RequestAttribute("id")` (set by `AuthInterceptor`), DTO no longer extends `UserRequestDto` (only `oldPassword`, `password`, validated), `@Valid` added, wrong old password → 400 (`IllegalArgumentException`), unknown id → 404, response 200. Spec §4 corrected. Tests: `AuthControllerTest` update-password cases (401 / own account + body email ignored / 400 wrong old / 400 invalid). Security reviewer: `id` not spoofable, fails closed |
| L5 | low | DEFER | (tests) S3–S5 and the 3 S6 methods are covered only by reflection (`RoleRequiredCoverageTest`), not by a request through the interceptor/security chain; `BusControllerAuthTest` uses `standaloneSetup`, so `SecurityConfig`/`WebConfig` wiring is not exercised. Matches the approved S8 ("reflection-check pattern"), so not a 0.5 defect | `RoleRequiredCoverageTest.java:79-84`; `BusControllerAuthTest.java` | deferred — add chain tests per controller when 1.1 / 3.2 wire those screens |
| L6 | low | DEFER | (tests) `ticketAdminEndpointsRequireAdmin` hard-codes 4 method names (new ADMIN methods on `TicketController` are not auto-covered), and nothing pins `createTicketByLoyalty` as intentionally ungated | `RoleRequiredCoverageTest.java:49-77` | deferred (4.3 — pin or lock when the loyalty rule is decided) |
| L7 | low | DEFER | (security) `GET /api/test-redis/ping` needs only any valid JWT and returns exception messages + internal network hints; `printStackTrace` | `RedisTestController.java:16-51` | deferred (B19) |
| L8 | low | DEFER | (security) `AuthInterceptor` returns `true` for non-`HandlerMethod` handlers; static `file:uploads/` is served without auth | `AuthInterceptor.java:40-42`; `application.properties:59` | deferred (B19 hardening) |
| L9 | low | DEFER | (backend) `AuthService` still throws raw `RuntimeException` (register catch, login, updatePassWord) — pre-existing lines | `AuthService.java:43-72` | deferred (B18) |
| L10 | low | DEFER | (backend) `booking_ticket` `RevenueClient` calls paths no controller maps | `RevenueClient.java:14-36` | deferred (B7) |
| L11 | medium | FIX | (fix round 1) `develop` does not compile its own tests: 0.4 committed `ManageRevenueTicketApplicationTests` (Testcontainers) and `TicketExcelSecurityChainTest` without the pom/interceptor they need (0.4 commit note "not self-contained"). 0.5's DoD also needs `@RestControllerAdvice` on `GlobalExceptionHandler` (else role denial → 500, not 403) and the rewritten `AuthInterceptor` (class-level `@RoleRequired`) | first reactor run on the new branch: `testCompile` failed, `org.testcontainers` missing | fixed on this branch — carried from `10b0bde`: `AuthInterceptor.java` + `AuthInterceptorTest.java`, `GlobalExceptionHandler.java`, Testcontainers test deps in `manage-revenue-ticket/pom.xml`, `mockito-extensions/MockMaker`. Not carried: Flyway (0.3), `RoleRequired` `@Inherited`, lombok version. These files are 0.4 prerequisites, to be marked "mixed" in the commit |
| L12 | high | NEEDS-USER | (security reviewer, fix round 1) Carrying the rewritten `AuthInterceptor` onto `develop` changes the default for endpoints with neither `@RoleRequired` nor `@PublicApi` from "403 for everyone" to "any valid JWT". On this branch a CUSTOMER can now call `BaseSalaryController` (POST/PUT/GET `/api/base_salary`), `SalaryController` (`/api/salary/driver`, GET/POST `/api/salary` with arbitrary `userId`), `LoyaltyPointsController`, `BaseLoyaltyPointsController`, `LoyaltyRewardController` (POST/PUT), `TicketController.createTicketByLoyalty`, `RedisTestController`. The same state already exists on the pushed `task/0.4-lock-public-endpoints` branch (`10b0bde`) | `AuthInterceptor.java:71-73`; `AuthInterceptorTest.java:77` (`allowsAuthenticatedRequestWhenEndpointHasNoRoleRestriction`); spec §4 row "Loyalty/Salary … không có `@RoleRequired`" | fixed (user decision (a), 2026-09-23) — `@RoleRequired(ADMIN)` on class + every handler of `SalaryController` (3), `BaseSalaryController` (3), `BaseLoyaltyPointsController` (2), `LoyaltyRewardController` (2), `RedisTestController` (1); touched files: constructor injection, `System.out`/`printStackTrace` → SLF4J. Tests: 5 controllers added to `RoleRequiredCoverageTest` (9 → 14 cases), new `SalaryControllerAuthTest` (CUSTOMER 403 / ADMIN 200 on `POST /api/salary`). `LoyaltyPointsController` + `createTicketByLoyalty` left for 4.3 (L13) |
| L13 | low | DEFER | (security reviewer, round 2) Still open to any logged-in role by user decision: `LoyaltyPointsController` POST/PUT `/api/loyalty_point` and `TicketController.createTicketByLoyalty`. Neither checks that ids in the body belong to the caller (IDOR-shaped once loyalty logic is live) | `LoyaltyPointsController.java:19-33`; `TicketController.java:96-105` | deferred (4.3 — decide role + ownership check with the loyalty rule) |
| L14 | — | NEEDS-USER | S9 live check not run: the backend on `:8082` runs pre-0.5 code; registering with `role=ADMIN` against it would create a real ADMIN. In-process substitute is now complete (`AuthControllerTest`, `BusControllerAuthTest`, `SalaryControllerAuthTest`), same stand-in accepted for 0.4 L7 | `curl :8082/api/bus` body = old interceptor path | needs-user — restart the backend from `task/0.5-register-role-required` and run S9, or accept the in-process tests |

Accepted decision, not a finding: locking `addTicket` / `updateTicket` /
`getTicketSummaryByBusAndTime` to ADMIN removes any COLLECTOR direct-sale path —
confirmed before §1.

### Commands run
- `git -C ticket-system archive 10b0bde | tar -x -C <scratchpad>/ts-10b0bde` (working
  tree and `~/.m2` untouched).
- `mvn -f <scratchpad>/ts-10b0bde/pom.xml -pl manage-revenue-ticket -am test` →
  **49/49, 0 failures, 0 errors, BUILD SUCCESS** (Testcontainers MySQL; Kafka
  "broker not available" warnings expected). 0.5 suites: `AuthServiceTest` 4,
  `RoleRequiredCoverageTest` 9, `BusControllerAuthTest` 4, `TicketControllerTest` 3.
- Not run: S9 manual curl (no running backend; see L2). `booking_ticket` not rebuilt
  (0.5 does not touch it; backend reviewer checked its Feign callers statically).

- Fix round 1 (branch `task/0.5-register-role-required`, working tree):
  first `mvn -f ticket-system/pom.xml -pl manage-revenue-ticket -am test` → testCompile
  failure on `develop`'s own test (L11); after carrying prerequisites and the L2/L4
  changes: `mvn -f ticket-system/pom.xml -pl manage-revenue-ticket -am clean test` →
  **42/42, BUILD SUCCESS** (`AuthControllerTest` 5, `BusControllerAuthTest` 5,
  `RoleRequiredCoverageTest` 9, `AuthServiceTest` 4, `AuthInterceptorTest` 7,
  `TicketExcelSecurityChainTest` 7, `TicketControllerTest` 3, `contextLoads` 1,
  `JwtAuthFilterTest` 1); `mvn -f ticket-system/pom.xml -pl booking_ticket -am
  test-compile` → OK; `mvn -f ticket-system/pom.xml install -DskipTests` → OK (installed
  `common-library` matches the branch). `curl :8082/api/bus` (no token) → 401 from the
  old interceptor path: running backend is pre-0.5 code. Live S9 not run (see L2).
- Review of fix round 1: independent `security-reviewer` on the round's diff → L4 fix
  sound; new L12.

- Round 2 (after the L12 fix): `mvn -f ticket-system/pom.xml -pl manage-revenue-ticket -am
  clean test` → **49/49, BUILD SUCCESS** (`RoleRequiredCoverageTest` 14,
  `SalaryControllerAuthTest` 2, `AuthControllerTest` 5, `BusControllerAuthTest` 5,
  `AuthInterceptorTest` 7, `TicketExcelSecurityChainTest` 7, `AuthServiceTest` 4,
  `TicketControllerTest` 3, `contextLoads` 1, `JwtAuthFilterTest` 1). `common-library` not
  changed since the fix-round-1 install. Not run: live S9 (L14).

### Open questions (NEEDS-USER)
1. L14 — run S9 live after restarting the backend on the 0.5 branch, or accept the
   in-process tests (as for 0.4 L7)?
2. Commit note (from L3/L11): the 0.5 commit will also carry 0.4 prerequisites
   (`AuthInterceptor` + test, `GlobalExceptionHandler`, pom Testcontainers deps,
   `MockMaker`) — mark them "mixed" in the commit message.

### Round history
| Round | Date | Result |
|---|---|---|
| Lead review 1 | 2026-09-23 | OPEN — 1 FIX open (L2); L1 fixed in-round by independent review; 6 DEFER (L5–L10); 2 NEEDS-USER (L3, L4); 49/49 tests green on `10b0bde` |
| Fix round 1 (`/plan-task 0.5`) | 2026-09-23 | L3 resolved (branch from `develop`), L2 + L4 fixed, L11 found and fixed (prerequisites carried), L12 new NEEDS-USER (high); 42/42 green |
| Fix L12 (user decision a) | 2026-09-23 | 5 controllers locked to ADMIN, tests added; independent security review clean; L13 deferred (4.3) |
| Lead review 2 | 2026-09-23 | **CLEAN** — 0 FIX open; 7 DEFER (L5–L10, L13); 1 NEEDS-USER (L14, live S9); 49/49 green |
