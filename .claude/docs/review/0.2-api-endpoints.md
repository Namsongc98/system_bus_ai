# Spec review 0.2: đồng bộ FE `API_ENDPOINTS` với path BE đã có

- Task: 0.2 (Phase 0, FE, size S), phụ thuộc 0.1 (đã tick `implement`/`test`/`review`)
- Nguồn: `booking_ticket_vue/src/constants/api_endpoint.js`, `src/services/*.js`,
  controller trong `ticket-system/*/controller/`, `Infrastructure/kong/kong.yml`
- Ngày review: 2026-09-21
- DoD trong plan: mỗi entry có comment trỏ `Controller.java:line` hoặc `// BE: missing`

URL thực tế được ghép như sau: `baseURL` = `VITE_KONG_API_URL` (`http://localhost:8000/api`) +
path của constant, rồi đi qua Kong (`strip_path: false`). `/api/booking` được định tuyến sang
booking `:8081`, mọi path `/api/*` còn lại sang manage-revenue `:8082`
(`kong.yml:53`, `kong.yml:79`). Trong tài liệu này, mọi path BE được ghi kèm tiền tố `/api`.

---

## 1. Summary

| Nhóm constant | Hiện tại | Sau 0.2 |
|---|---|---|
| `AUTH.LOGIN`, `REGISTER`, `UPDATE_PASSWORD` | `READY` | Giữ nguyên, thêm comment |
| `AUTH.ME`, `LOGOUT`, `REFRESH` | `MISSING_BE` | Giữ nguyên, `// BE: missing` (ME → task 0.6) |
| `BOOKING.BASE` | `READY` | Thêm comment |
| `ADMIN.*` (2) | `READY` | Thêm comment |
| `REVENUE.*` (4) | `READY` (path); contract là việc của 3.2 | Thêm comment |
| `BUSES.BASE`, `BUSES.BY_ID` | `MISMATCH` (`/buses` ≠ `/bus`) | → `/bus`, `/bus/{id}` |
| `ROUTES.BASE`, `ROUTES.BY_ID` | `MISMATCH` (`/routes` ≠ `/route`) | → `/route`, `/route/{id}` |
| `TRIPS.BASE`, `TRIPS.BY_ID` | `MISMATCH` (`/trips` ≠ `/trip`) | → `/trip`, `/trip/{id}` |
| `TICKETS.BASE` | `MISMATCH` (`/tickets` ≠ `/ticket`) | → `/ticket` |
| `SALARY.BASE_SALARY` | `MISMATCH` (`/base-salary` ≠ `/base_salary`) | → `/base_salary` |
| `SALARY.BASE` | `UNKNOWN_CONTRACT` (BE bắt buộc `month`, `year`) | Thêm comment, ghi rõ param bắt buộc |
| `USERS.*`, `SEATS.*`, `PAYMENTS.*`, `TRIPS.SEARCH/COMPLETE`, `TICKETS.MINE/BY_ID`, `LOYALTY_*`, `SALARY.CALCULATE` | `MISSING_BE` | Giữ nguyên path, `// BE: missing (task X.Y)` |

Đếm theo từng constant (40 entry): `READY` 10 · `MISMATCH` 8 (sửa được trong 0.2) · `UNKNOWN_CONTRACT` 1 · `MISSING_BE` 21.

Cách hiểu 1 constant dùng chung cho nhiều method (ví dụ `BUSES.BASE` dùng cho cả `GET` và `POST`):
chỉ đổi path khi BE **có** resource đó cho ít nhất 1 method mà FE đang gọi. Method nào BE chưa có
thì ghi `BE: missing` ngay trong comment của method đó.

---

## 2. FE-BE readiness matrix

Status theo `.claude/skills/fullstack-page-api-review/references/review-output-template.md`.
Viết tắt: `MR` = `manage-revenue-ticket/.../controller/`, `BK` = `booking_ticket/.../controller/`.

| Constant | Nơi FE gọi (method) | Request thực tế hiện tại | BE evidence | Status | Việc cần làm |
|---|---|---|---|---|---|
| `AUTH.LOGIN` | `authService.js:6` POST | `/api/auth/login` | `MR/AuthController.java:65` | `READY` | Comment |
| `AUTH.REGISTER` | `authService.js:9` POST | `/api/auth/register` | `AuthController.java:38` | `READY` | Comment |
| `AUTH.UPDATE_PASSWORD` | `authService.js:21` PUT | `/api/auth/update-password` | `AuthController.java:52` | `READY` | Comment |
| `AUTH.LOGOUT` | `authService.js:12` POST | `/api/auth/logout` | — | `MISSING_BE` | `// BE: missing` (plan: logout chỉ xoá token FE) |
| `AUTH.REFRESH` | `authService.js:18` POST | `/api/auth/refresh` | — | `MISSING_BE` | `// BE: missing` (Phase 4) |
| `AUTH.ME` | `authService.js:15` GET | `/api/auth/me` | — | `MISSING_BE` | `// BE: missing (0.6)` |
| `USERS.BASE` | `userService.js:6` GET, `:15` POST | `/api/users` | `UserController.java` là class rỗng | `MISSING_BE` | `// BE: missing (1.2)` |
| `USERS.BY_ID` | `userService.js:9` GET, `:21` PUT, `:24` DELETE | `/api/users/{id}` | — | `MISSING_BE` | `// BE: missing (1.2)` |
| `USERS.PROFILE` | `userService.js:12` GET, `:18` PUT | `/api/users/profile` | — | `MISSING_BE` | `// BE: missing (4.1)` |
| `BOOKING.BASE` | `bookingService.js:6` POST | `/api/booking` → booking | `BK/BookingController.java:10,16` | `READY` | Comment |
| `TRIPS.BASE` | `tripService.js:9` GET, `:15` POST | `/api/trips` | POST `MR/TripController.java:24`; GET list chỉ có `/api/trip/scheduled` (`:30`) | `MISMATCH` | → `/trip`; comment: POST ✓, GET list `BE: missing (1.3)` |
| `TRIPS.BY_ID` | `tripService.js:12` GET, `:18` PUT, `:24` DELETE | `/api/trips/{id}` | PUT `TripController.java:41`; GET/DELETE không có | `MISMATCH` | → `/trip/{id}`; comment: PUT ✓, GET/DELETE `BE: missing (1.3)` |
| `TRIPS.SEARCH` | `tripService.js:6` GET | `/api/trips/search` | — | `MISSING_BE` | `// BE: missing (2.1)` |
| `TRIPS.COMPLETE` | `tripService.js:21` PUT | `/api/trips/{id}/complete` | — (plan 1.3 dùng `PATCH /trip/{id}/status`) | `MISSING_BE` | `// BE: missing (1.3)` |
| `SEATS.BY_TRIP` | `seatService.js:6` GET | `/api/trips/{id}/seats` | — | `MISSING_BE` | `// BE: missing (2.2)` |
| `SEATS.BY_ID` | `seatService.js:9` GET | `/api/seats/{id}` | — | `MISSING_BE` | `// BE: missing` (không có task nào trong plan) |
| `TICKETS.BASE` | `ticketService.js:6` GET, `:15` POST `create`, `:18` POST `book` | `/api/tickets` | POST `MR/TicketController.java:37`; GET list không có | `MISMATCH` | → `/ticket`; comment: POST ✓, GET `BE: missing (4.2)` |
| `TICKETS.MINE` | `ticketService.js:9` GET | `/api/tickets/my` | — | `MISSING_BE` | `// BE: missing (2.4)` |
| `TICKETS.BY_ID` | `ticketService.js:12` GET, `:21` PATCH `/cancel` | `/api/tickets/{id}` | — | `MISSING_BE` | `// BE: missing (2.3, 2.4)` |
| `PAYMENTS.*` (4; `BASE` không được service nào dùng) | `paymentService.js:6,9,12` | `/api/payments/...` | Không có module payment | `MISSING_BE` | `// BE: missing (2.3)` |
| `BUSES.BASE` | `busRouteService.js:7` GET, `:13` POST | `/api/buses` | GET `MR/BusController.java:24`, POST `:38` (mapping `"api/bus"` thiếu `/` đầu nhưng Spring vẫn chuẩn hoá thành `/api/bus`) | `MISMATCH` | → `/bus` |
| `BUSES.BY_ID` | `busRouteService.js:10` GET, `:16` PUT, `:19` DELETE | `/api/buses/{id}` | PUT `BusController.java:44`; GET/DELETE không có | `MISMATCH` | → `/bus/{id}`; comment: GET/DELETE `BE: missing (1.1)` |
| `ROUTES.BASE` | `busRouteService.js:26` GET, `:32` POST | `/api/routes` | POST `MR/RouteController.java:18`; GET không có | `MISMATCH` | → `/route`; comment: GET `BE: missing (1.1)` |
| `ROUTES.BY_ID` | `busRouteService.js:29` GET, `:35` PUT, `:38` DELETE | `/api/routes/{id}` | PUT `RouteController.java:25` | `MISMATCH` | → `/route/{id}`; comment: GET/DELETE `BE: missing (1.1)` |
| `ADMIN.DASHBOARD` | `adminService.js:6` GET | `/api/admin/dashboard` | `MR/AdminDashboardController.java:27` | `READY` | Comment |
| `ADMIN.REVENUE` | `adminService.js:10` GET | `/api/admin/revenue` | `AdminDashboardController.java:37` | `READY` | Comment |
| `REVENUE.REPORT/BY_ROUTE/BY_DATE/TOP_CUSTOMERS` | `revenueService.js:6,19,32,45` GET | `/api/revenue/...` | `MR/RevenueController.java:76,82,88,94` | `READY` (path) | Comment; response là `ResponseEntity<?>`/Map, xử lý ở 3.2 |
| `LOYALTY_POINTS.BASE` | `loyaltyService.js:6` GET | `/api/loyalty-points` | BE `/api/loyalty_point` chỉ có POST `:19`, PUT `:26` | `MISSING_BE` | `// BE: missing (4.3)` |
| `LOYALTY_POINTS.EARN/REDEEM` | `loyaltyService.js:19,32` POST | `/api/loyalty-points/earn\|redeem` | Không có; POST `/api/loyalty_point` là "tạo lịch sử điểm", khác nghĩa | `MISSING_BE` | `// BE: missing (4.3)` |
| `LOYALTY_REWARDS.BASE` | `loyaltyService.js:45` GET | `/api/loyalty-rewards` | BE `/api/loyalty/rewards` chỉ có POST `:17`, PUT `:23` | `MISSING_BE` | `// BE: missing (4.3)` |
| `SALARY.BASE` | `salaryService.js:6` GET | `/api/salary` | `MR/SalaryController.java:29`, bắt buộc `month`, `year` | `UNKNOWN_CONTRACT` | Comment ghi rõ `month`/`year` bắt buộc; không có màn hình (4.4 hoãn) |
| `SALARY.CALCULATE` | `salaryService.js:19` POST | `/api/salary/calculate` | Không có; BE có POST `/api/salary/driver` (`:20`) và POST `/api/salary?userId` (`:41`), tham số khác | `MISSING_BE` | `// BE: missing (4.4)` |
| `SALARY.BASE_SALARY` | `salaryService.js:32` GET | `/api/base-salary` | `MR/BaseSalaryController.java:32`; trả `List` thô, **không** bọc `BaseResponseDto` | `MISMATCH` | → `/base_salary`; comment ghi rõ response không có envelope |

BE có nhưng FE chưa có constant (`MISSING_FE`), không thuộc 0.2:
`GET /api/trip/scheduled`, `GET /api/trip/{id}/revenue` (1.3); `GET /api/ticket/summary`,
`GET /api/ticket/summary/excel` (3.2); `/api/revenue/{bus,employee,user,bus/all,bus/{id}/range}`;
`/api/base_loyalty_point`; `POST /api/ticket/loyalty_reward/{id}` (4.3).

---

## 3. Fix scope cho `plan-task 0.2`

### BE
Không có. 0.2 chỉ sửa FE. Không thêm và không sửa endpoint nào.

### FE — `booking_ticket_vue/src/constants/api_endpoint.js`

| ID | Việc |
|---|---|
| S1 | `BUSES.BASE` `/buses` → `/bus`; `BUSES.BY_ID` → `/bus/${id}` |
| S2 | `ROUTES.BASE` `/routes` → `/route`; `ROUTES.BY_ID` → `/route/${id}` |
| S3 | `TRIPS.BASE` `/trips` → `/trip`; `TRIPS.BY_ID` → `/trip/${id}` |
| S4 | `TICKETS.BASE` `/tickets` → `/ticket` |
| S5 | `SALARY.BASE_SALARY` `/base-salary` → `/base_salary` |
| S6 | Mỗi entry có 1 comment theo dạng `// GET ✓ BusController.java:24 · DELETE BE: missing (1.1)`. Entry `MISSING_BE` giữ nguyên path và ghi `// BE: missing (task X.Y)`. Riêng `SALARY.BASE` ghi `month, year required`; `SALARY.BASE_SALARY` ghi `no BaseResponseDto envelope` |
| S7 | Không đổi **tên** constant nào, không xoá entry nào, không đổi service/store/page. Mẫu `${API_ENDPOINTS.TICKETS.BY_ID(id)}/cancel` (`ticketService.js:21`) giữ nguyên |

### Docs
| ID | Việc |
|---|---|
| S8 | Cập nhật cột "path FE hiện tại" trong 9 file `.claude/references/frontend/api/*/endpoints.md` đang ghi `/buses`, `/routes`, `/trips`, `/tickets`, `/base-salary` (ví dụ `admin-buses-routes/endpoints.md:15,26,32,33`). Chỉ sửa path, không sửa đánh giá contract |

### Tests
| ID | Việc |
|---|---|
| S9 | Thêm `booking_ticket_vue/src/__tests__/apiEndpoints.spec.js`: assert path của S1–S5 (ví dụ `API_ENDPOINTS.BUSES.BY_ID(5) === '/bus/5'`), và assert các path `READY` không bị đổi (`AUTH.LOGIN`, `BOOKING.BASE`, `ADMIN.*`, `REVENUE.*`) |
| S10 | Verify: `npm run test:unit -- --run` (gồm cả `apiBaseUrl.spec.js` đã có) và `npm run build`. Nếu Kong/BE đang chạy thì smoke test `GET /api/bus?page=0&size=10` có token ADMIN phải trả 200; nếu chưa chạy thì ghi rõ là bỏ qua |

Hệ quả cần biết khi nghiệm thu: sau S1–S4, các call mà BE chỉ thiếu method sẽ đổi từ lỗi **404**
sang **405 Method Not Allowed**. Ví dụ `GET /api/trip`, `DELETE /api/bus/{id}`, `GET /api/route`.
Đây là hành vi đúng, không phải bug mới, và sẽ hết khi làm 1.1/1.3.

---

## 4. Out of scope

| Phát hiện | Thuộc task |
|---|---|
| `POST /api/ticket` (`TicketController.java:37`) nhận `customerId`/`price` từ body, không có `@RoleRequired`. Sau S4, `ticketService.book` (đang không được page nào gọi: `bookingStore.bookTicket` không có caller) sẽ trỏ vào endpoint này | 2.2 (đổi luồng đặt vé sang `POST /api/booking`), 0.5 |
| `BusController` `GET` trả `Page<Buses>` entity, map `response` được tạo ra rồi bỏ không dùng (`BusController.java:28-32`) | 1.1 |
| `BaseSalaryController.getAll` trả `List` không có envelope, trái quy ước `BaseResponseDto` | 4.4 |
| `salaryService`/`loyaltyService` ném `{code}` trong khi BE trả `status` (`_conventions.md` mục 4) | 4.3 / 4.4 |
| `SEATS.BY_ID` không có task nào trong plan | Đề xuất bỏ ở 2.2 nếu không dùng |

---

## 5. Đề xuất sửa plan

1. Dòng B2 ghi BE path là `/api/loyalty_point` và `/api/base_salary`, nhưng rewards thật là
   **`/api/loyalty/rewards`** (không phải `loyalty_reward`), ngoài ra còn có `/api/base_loyalty_point`.
   Nên bổ sung vào B2 và 4.3.
2. Phase 0, dòng 0.2: nên ghi rõ quy tắc "constant dùng chung nhiều method → đổi path khi BE có ít
   nhất 1 method" và hệ quả 404 → 405 nêu ở mục 3, để task 1.x không hiểu nhầm là lỗi hồi quy.
3. 4.4 Salary: FE `SALARY.CALCULATE` không khớp với 2 endpoint POST thật (`/salary/driver`,
   `/salary?userId`). Nên ghi vào 4.4 để khi làm lại không đoán sai.

---

## 6. Open decisions

1. `(TODO/needs confirmation)` Entry `MISSING_BE` mà plan **đã chốt path đích**
   (`USERS` → `/user` ở 1.2, `SEATS.BY_TRIP` → `/trip/{id}/seats` ở 2.2, `TRIPS.SEARCH` →
   `/trip/search` ở 2.1, `TICKETS.MINE` → `/ticket/me` ở 2.4): trong 0.2 nên
   **(A)** giữ path cũ, chỉ gắn `// BE: missing` (đúng nguyên văn plan), hay **(B)** đổi luôn sang path
   đích, kèm `// BE: missing (planned X.Y)`?
   Mặc định trong scope trên là **A**.
