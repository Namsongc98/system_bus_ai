---
description: Create, fill, or refactor a reusable Base/Common component from Figma HTML or existing markup.
name: prompt-frontend-reuse-component
argument-hint: Pattern A or B, component name/path, then paste Figma HTML or existing markup.
agent: agent
---

Apply the [frontend-reuse-component](../skills/frontend-reuse-component/SKILL.md) skill.

Use the shared component patterns and registry linked by the skill. Keep the component props-in/events-out and reuse existing Base/Common components before creating new UI.

## Component request

- Pattern: A primitive element / B common composed component
- Component name:
- Target path:
- Current file state: empty / existing component / refactor existing markup

## Contract hints

- Expected props:
- Expected emits:
- Expected slots:
- Existing components to reuse, if known:

## Figma HTML or existing markup

```html
<!-- paste Figma HTML/CSS or existing markup here -->
```

## Output

- Implement or provide the component.
- Summarize the component contract and reused dependencies.
